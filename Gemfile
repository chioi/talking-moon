source "https://rubygems.org"
require 'rake'

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in chat_server.gemspec
Gem::Specification.new do |gemspec|
  gemspec.authors = 'Mario Gil'
  gemspec.files = FileList['lib/**/*.rb'].to_a
  gemspec.name = 'chat_server'
  gemspec.name = 'A small chat server'
  gemspec.version = '0.1.pre'
end
